export default function vuCore(): string {
    return "This is fake content from ts"
}

import {TextTrack} from "vualto-test-player-models";

let textrack: TextTrack = {
    label: "youpi",
    id: 12,
};